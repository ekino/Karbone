# Temporary GitLab CI for the internal review. The reference CI lives in .github/workflows (GitHub Actions).
# Integration specs (Testcontainers) skip themselves when Docker is unavailable on the runner.

workflow:
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_TAG
    - if: $CI_PIPELINE_SOURCE == "web"

default:
  image: registry.ekino.com/iperia/docker-build/ci-kotlin-2_4-java25:2026.06.05
  tags:
    - "aws"
    - "docker"
    - "ekino"
    - "france"

variables:
  GRADLE_USER_HOME: "$CI_PROJECT_DIR/.gradle-home"
  GRADLE_OPTS: "-Dorg.gradle.daemon=false"

cache:
  key:
    files:
      - gradle/libs.versions.toml
      - gradle/wrapper/gradle-wrapper.properties
  paths:
    - .gradle-home/caches
    - .gradle-home/wrapper
    - .gradle-home/jdks

build:
  stage: build
  script:
    - chmod +x ./gradlew
    - ./gradlew build --console=plain
  artifacts:
    when: always
    expire_in: 1 week
    reports:
      junit: build/test-results/test/TEST-*.xml
    paths:
      - build/reports/
