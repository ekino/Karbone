# Copy to .env (gitignored) to run the integration tests that need secrets.
# Gradle's test task loads this file; real environment variables take precedence.

# Carbone Cloud *test* API key: enables CarboneCloudSpec against api.carbone.io (pdf output only).
CARBONE_TEST_API_KEY=
