Test-only ES512 key pair generated with `carbone/carbone-ee generate-keys` / `generate-token` for `CarboneAuthContainerSpec`.
The private key is not kept. The token is valid ~40 years and only works against a container started with this public key.
